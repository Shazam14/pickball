// Walk-in v1 constants. Edit values here; no other places reference them.
export const WALKIN_FEE_PHP = 50
export const WALKIN_COUPON_PHP = 50              // single-use, knocks ₱50 off any cafe/merch line
export const WIFI_SSID = 'SideOut-Guest'         // placeholder — swap for real network
export const WIFI_PASSWORD = 'sideout2026'       // placeholder — swap for real password
export const WIFI_FREE_MINUTES = 60              // honor-system in v1; printed on ticket
